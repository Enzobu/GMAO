info:
  name: get all
  type: http
  seq: 2

http:
  method: GET
  url: "{{apiUrl}}{{apiPrefix}}/maintenances"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{API_Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
