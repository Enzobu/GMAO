# API contracts rules

- Keep API contracts explicit and stable.
- Do not change request or response shapes without mentioning it.
- Update shared contracts when API payloads change.
- Update frontend API types when backend responses change.
- Keep DTOs, validation schemas, and frontend types aligned.
- Mention breaking changes clearly.
- Do not silently rename fields used by clients.