# Routes API

Les routes API sont préfixées par `/api`.

Route d'authentification :

```text
POST /api/login
```

Les autres routes sont exposées par API Platform selon les ressources métier
configurées dans l'application.
